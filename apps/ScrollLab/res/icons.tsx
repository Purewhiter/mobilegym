import { ArrowLeft, ListChecks } from 'lucide-react';

export const IcLauncher = ListChecks;
export const IcBack = ArrowLeft;

export const ICON_REGISTRY: Record<string, unknown> = {
  IcLauncher,
  IcBack,
};
