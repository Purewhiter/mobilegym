[pytest]
asyncio_mode = auto
; Live tests need a running simulator at localhost:3000; deselect them by
; default so a bare `pytest` never hangs. Opt in with `-m live` (or -m "").
addopts = -n 3 -m "not live"
required_plugins = pytest-xdist
